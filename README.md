
  # v1 CIELO AGENCY OS

  This is a code bundle for v1 CIELO AGENCY OS. The original project is available at https://www.figma.com/design/o7zR0lrknk6syTixfn4ml9/v1-CIELO-AGENCY-OS.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  